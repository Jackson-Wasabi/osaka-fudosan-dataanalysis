# 判断理由ログ (decision_log)

## 2026-06-15 — Step 9 mart の設計判断

- **D-021（採用）** mart_condo_price は両Foldを保持し fold/split 列で区別（Tableauは fold='A' でフィルタ）。理由: PART17のFold横断モデル比較・配点感度分析を mart 上で完結させ、本番(A)と検証(B)を1テーブルで扱う。物件が2回出るためTableau側のフィルタ前提を docs/README に明記する。
- **D-017 確定（mart反映）** 新規出現駅（駅中央値が訓練に無くNULL）は mart に残しつつ `station_history_missing=TRUE`・`model_eligible=FALSE` で識別し、モデル学習・予測・候補ランキングから除外。理由: 駅相場が無い物件は手法（駅相対価格）の前提を満たさず推定不能。区中央値フォールバックは「駅相場」の意味を薄めるため不採用。件数 Fold A test 9・Fold B test 8（計17・約0.2%）。
- **D-005 実装（実装漏れの補完）** potential_dup_flag を stg_transactions に実装（TO_JSON_STRINGで全21列一致を判定・COUNT OVER>1）。決定済みだが未実装だったものを Step 9 で補完。Fold A で182件。
- mart 設計: 予測特徴量は補完済みの値を正規名（building_age_years/walk_minutes）で公開し生のNULL列は出さない（Step10で誤って生列を使う事故を構造的に防止）。目的変数 log_price_per_sqm を追加。

## 2026-06-15 — 耐震フラグの一貫性修正（整合性監査の結果）

- **D-020（採用）** 旧耐震/新耐震フラグの定義を「補完後築年ベース」に一本化。
  - 問題: `seismic_old_flag` を staging で生の built_year から作っていた（欠損→0）ため、築年を補完して旧耐震になった行が `seismic_new=0`（旧耐震）かつ `seismic_old_flag=0`（旧耐震でない）と矛盾（Fold A 9行）。リスク減点の判定列で結論が変わる不整合。
  - 修正: staging から seismic_old_flag を削除し、intermediate で `seismic_new`（推定建築年>=1982）と `seismic_old_flag`（同<1982）を**補完後築年から相補的に算出**。検証で `seismic_new + seismic_old_flag = 1`（矛盾0行）を確認。
  - 誠実さ: 補完(推定)由来の旧耐震は `is_imputed_building_age` で区別でき、リスク評価で「推定の旧耐震」と「確定の旧耐震」を分けられる。
  - リスク減点の「旧耐震」は今後この `seismic_old_flag`（補完後ベース）で判定する。

## 2026-06-15 — Step 8 欠損補完の設計判断

- **D-018（採用・ユーザー承認）** 欠損補完は「列ごとに理由のある方法」で実装（一律中央値にしない）。すべて当該Foldの訓練期間(split='train')のみから `PERCENTILE_CONT(IF(split='train',値,NULL),0.5)` で中央値を算出しリーク防止。
  - **築年数**: 駅→区→全体の中央値。理由: 欠損476件は全て空欄＝真の不明（戦前0件と確認済み）で、位置が近い物件は建築年代が近い。右に歪む分布のため平均でなく中央値。`is_imputed_building_age` 付与。
  - **徒歩分**: 駅→区の中央値。理由: 徒歩分は住所→駅の地理的距離で、同じ駅周辺の典型距離が最良の代理。当初案の「駅×面積帯」は面積と徒歩の相関が弱く根拠が薄いため不採用。`is_imputed_walk_minutes` 付与。
  - **公示価格**: 補完ではなく最近傍計算の対象を全取引駅に拡大（int_station_land_price_features）。理由: 欠損4駅(なにわ橋/大阪難波/柴島/汐見橋)は座標を持ち、中央値補完より実計算が正確。地価は単年スナップショットの地理データで時点リークなし・予測モデルにも入れない。座標が無い駅のみ区中央値フォールバック（`is_imputed_land_price`、実績0件）。
  - 補完後の築年数から `seismic_new` を再計算。補完値は範囲内に収まることを確認（築5-58年・徒歩1-20分）。
  - 効果: complete-case 7,796 + 駅ありで徒歩/築年欠損の116行を補完で復活。補完後の主要列NULL=0。
- **D-019（採用）** scope に「最寄駅名あり」を必須化（手順書 PART 7: 最寄駅名欠損は基本除外）。理由: 駅名がない物件は駅中央値㎡単価・駅地価・駅ランキングのいずれにも乗れず分析不能。当初 scope を欠損許容にした際に駅名空欄314行が混入し駅中央値・地価がNULLになる不具合を検出→修正。駅名空欄は excluded_reason='station_missing' で除外。complete-case 7,796 は全て駅名ありのため減少なし。scope は 7,912（=7,796+補完対象116）。
- **D-017 更新** テスト期間に新規出現する駅（station_median_price_per_sqm が訓練に無くNULL）は Fold A test 9件・Fold B test 8件。地価は実計算で付くが駅中央値は欠ける。Step 9 mart / Step 10 モデルで「除外」か「区中央値フォールバック」を決定する（未決）。

## 2026-06-15 — Step 8 intermediate の設計判断

- **D-012（採用・ユーザー承認「Fold対応」）** 駅市場特徴量（station_median_price_per_sqm 等）のリーク対策を「Fold対応」で実装。int_station_market_features を粒度 駅×Fold とし、各 Fold の**訓練期間のみ**から中央値・IQR・平均徒歩・改装率を算出（Fold A 訓練=2021-2024 / Fold B 訓練=2021-2023）。中央値・IQR は GROUP BY 可能な APPROX_QUANTILES で算出。これを各 Fold の train/test 両方に適用しテスト期間の情報混入を防ぐ（D-009 の具体化）。
- **D-013（採用）** 駅名正規化を dbt マクロ `normalize_station_name`（括弧除去→「駅」除去）に共通化し、stg_station_master と stg_transactions の両方で使用。さらに **stg_transactions に正規化駅名 `station_name` を追加**（従来は station_name_raw のみで取引↔駅マスタが結合不能だった。PART 7 の station_name_clean 相当）。効果: スコープ内取引の駅結合率 **100%**。
- **D-014（採用・バグ修正）** scope_flag の NULL 誤判定を修正（`COALESCE(条件, FALSE)` で NULL→対象外を確定）。詳細は errors_and_fixes.md。結果 scope_true=**7,796** で D-011 と一致。
- **D-015（採用・バグ修正）** stg_land_price の大阪市抽出を L01_006（連番・誤）→ L01_001 が '271' 始まり（行政区域コード）に修正。市区名は住所 L01_025 から抽出。574地点。
- **D-016（採用）** 駅の代表座標を共通テーブル int_station_geo に集約し、**大阪府バウンディングボックスに限定**してから平均（同名駅の全国平均ズレ対策）。地価最近傍結合・物件結合の両方が参照。
- **D-017（記録のみ・既知の限界）** テスト期間に新規出現した駅（2025のみ等）は当該Foldの訓練に履歴がなく、駅市場特徴量・地価特徴量が NULL になる（Fold A test で9件/2,071、0.4%）。D-009 の必然的帰結。Step 9 mart / Step 10 モデルで「除外」か「区中央値等でのフォールバック」を決める。
- **接続の運用メモ**: 本環境では gcloud ハングのため `GOOGLE_APPLICATION_CREDENTIALS`(ADCコピー)+`GOOGLE_CLOUD_PROJECT` を設定して dbt/BQ を実行する（errors_and_fixes.md 参照）。

## 2026-06-12 — Step 5 EDA: 外れ値・欠損の処理方針（D-ID制・すべて提案中）

以後、staging 実装のSQLコメント・eda_report.md とこのIDで相互参照する。

- **D-001（不採用）** 大阪市スコープでは範囲表記が1件（1H30～2H）のみ、かつ明確に20分超。Step6の「徒歩20分以内」フィルターで自動除外されるため、stgでの変換処理は不要と判断。
- **D-002（採用）** 外れ値の除外は「物理的に説明できるミスのみ」に限定し、IQRフェンス自動フラグ（outlier_pps_flag）は不採用。具体的な除外対象: 面積100m²超（D-003）・㎡単価5万円未満。グレーゾーン（天神橋筋六丁目964万/m²等）は証明手段がないため残す。モデルへの影響は目的変数のlog変換（log_price_per_sqm）で抑制する（案A+B採用）。
- **D-003（採用）** 面積100m²超は scope_flag=FALSE・excluded_reason='area_out_of_scope'。100m²超の除外閾値のみ今回確定。分析に使う実際の上限（20〜55m²等）はStep 6で別途確定する。
- **D-004（不採用）** 大阪市スコープでは「戦前」表記が0件のため実装不要と判断。
- **D-005（採用）** 全21列が一致する行219件（0.9%）を全件保持し `potential_dup_flag=1` で識別するにとどめる。理由: データに部屋番号・フロア・正確な住所がなく「二重登録」と「別部屋の同時成約」を区別できない。誤って集約した場合は実在する別取引を消す不可逆なミスになるのに対し、重複を残した場合は駅別中央値・件数への影響が0.9%にとどまり軽微。誤りの方向性として「残す」の方が害が小さいと判断。
- **D-006（採用・案A）** 駅名正規化は (1)括弧除去 (2)「駅」除去 (3)mapping CSV の3段階。rawの表記をそのまま保持し、stgで変換する。mapping CSV 7行: 四天王寺前夕陽ケ丘→四天王寺前夕陽ヶ丘 / ＪＲ難波→JR難波 / なんば→難波 / あびこ→我孫子 / 鶴ケ丘→鶴ヶ丘 / ＪＲ淡路→JR淡路 / ＪＲ野江→JR野江。mapping後は駅名での不一致0件の見込み。残る station_joined=0 は駅名が空欄の469行のみ。徒歩分の信用性はmappingの影響を受けない（同一物理駅の表記変換のみ）。
- **D-007（採用）** 改装空欄23,558件（95.7%）は renovation_unknown_flag=1。改装済み=renovation_done_flag=1。「未改装」という値がデータに存在しないため空欄を未改装と断定しない。renovation_unknown_flagは予測モデルに入れず、データ品質スコア・リスク減点で使う。
- **D-008（採用・案B）** 築年数マイナスは2件（0.01%）のみ（min=-2）。竣工前売買か誤記と判断。building_age_years=0 に補正 + is_negative_age=1 フラグを付与してscope内に残す。モデルへの悪影響を防ぎつつデータ損失を最小化。is_negative_ageはモデル特徴量には入れず、リスク減点 -2点の材料とする。

- **D-009（採用・案A+B）** リーク防止として訓練期間ベースの時系列分割 + ウォークフォワード検証を併用する。Fold A（本番）: 訓練=2021-2024・テスト=2025。Fold B（検証）: 訓練=2021-2023・テスト=2024。駅別中央値・IQR等のデータ依存統計量は各Foldの訓練期間のみで算出しテスト期間に適用する。両FoldのMAEを比較し年をまたいだ安定性を確認する。面積100m²・価格500-8000万円等の業務固定閾値はリーク懸念なし。
- **D-010（採用済み・実施済み・ユーザー指摘起点）** 分布・外れ値のEDAは分析対象スコープ（大阪市）で実施する。府全体は形式チェック（範囲表記・戦前・重複等の洗い出し）のみに使う。同日中に大阪市版で全面再実行済み。**効果の実証: ㎡単価P99が府全体158万→大阪市186万円/m2**と大きく異なり、府全体閾値では大阪市の正常な高額物件を誤って外れ値扱いするところだった。
- **D-011（採用）** 分析スコープ確定: **大阪市・面積20-60m²・徒歩20分以内・築5-60年・価格500万円以上（上限なし）**。残件数7,796件・10件以上の駅134駅（旧プロジェクト72駅の約1.9倍）。
  - 価格上限を設けない理由: 上限で打ち切ると北区・中央区等の都市部駅で駅別成約中央値が実態より低く算出され、割安度スコアの基準が歪む。価格の外れ値はIQRフェンスフラグ（D-002）で別途管理する。
  - 面積下限20m²: 15-19m²はトランクルーム・特殊区分所有・事務所転用の可能性があり居住用マンションとして実態不明のため除外。
  - 面積上限60m²: 単身・DINKS・投資用テーマに合致しつつ、55m²上限（市場の28%のみカバー）より2.4倍の件数を確保。
  - 築年数下限5年: 築0-4年は新築プレミアム（デベロッパー利益・広告費）が価格に上乗せされており中古相場と乖離するため除外。
  - 築年数上限60年: P99=53年をカバーしつつmax76年の外れ値を除外。旧耐震（築45年以上）はスコアの減点で対応（CLAUDE.md参照）。

### Step 6 確定条件（D-011）
確定条件（大阪市・面積20-60m²・徒歩20分以内・築5-60年・価格500万円以上・上限なし）で **7,796件・駅別10件以上が134駅** 残る。
旧プロジェクト（72駅）の約1.9倍の母数を確保。価格上限撤廃により旧条件（500-8000万）比で51件増。

> 注（2026-06-15・実装後の更新）: この時点の数値は補完前・生駅名ベース。実装後の正は **D-019** 参照。
> scope=**7,912**（徒歩/築年の欠損を補完で復活させた116行を含む）／補完なしの complete-case=**7,796**（=本決定値）／
> 駅名は D-006 正規化で同名統合され **126駅**（134は正規化前の生駅名ベース）。論理は不変で数値表記のみ更新。

## 2026-06-12 — Step 3〜4: 投入方式の判断

### 判断1: GCPプロジェクトを新規作成（osaka-fudosan-dataanalysis）
- ユーザー指示。旧プロジェクト osaka-condo-analysis とテーブルが混ざらず、v9の成果物が独立して説明できる。課金は同一アカウントに紐付け（後のBQMLに必要）。

### 判断2: raw_transactions は大阪府全体47,386行を1テーブルに as-is 投入
- v9手順書の原則「rawは元CSVをそのまま保存」に従い、大阪市への絞り込みは staging で行う（旧作業は投入時に大阪市へ絞っていたが、絞り込み条件の変更に弱いため変更）。
- 5年分は同一スキーマのため1テーブルに追記ロード。年は trade_period 列から復元可能で情報損失なし。

### 判断3: 駅マスタ・地価は年別テーブル（_2024/_2025）で分離投入
- ユーザー指示「統合せず」に従う。年を跨ぐ結合・差分確認は staging/intermediate で行う。

### 判断4: 全列 STRING でロード
- 型解釈（例: コード"01"→数値1で先頭ゼロ消失）による意図しない値の変化を防ぐ。型変換は staging に集約し、変換ロジックを dbt で説明可能にする。

### 判断5: raw 列名は英語 snake_case に置換（位置対応）
- BigQuery の列名制約で日本語ヘッダーの「：」「（）」が使用不可のため。元の日本語カラム名との対応は data/samples/README.md のカラム一覧が対応表を兼ねる。

### 判断6: GIS は CSV（lon/lat列）方式、GEOGRAPHY型は不採用
- 最終出力が Tableau 地図のため lat/lon 数値列が最も確実。距離計算は staging で ST_GEOGPOINT を作れば可能。
- 駅の代表点は LineString 座標列の中央要素による簡易中点（数十m程度の近似）。駅単位集計・1000m圏地価の用途では影響なし。元GeoJSONは data/gis/raw に保持。

### 判断7: 成約CSVは cp932→UTF-8 の変換コピー（TEMP）でロード
- BigQuery が cp932 を読めないため。raw ファイルは無変更で保持し、変換は文字コードのみ（内容無変更）。

## 2026-06-12 — rawデータはコミットせず、data/samples の抜粋で代替

- 成約価格情報は不動産流通機構由来で、GitHub での再配布が規約上明確に許可されているか不明なため、raw 全体はリポジトリに含めない。
- 代替として (1) 取得スクリプト（scripts/）で再現性を担保、(2) data/samples に先頭10行＋出典・カラム説明を置き、閲覧者がデータ構造を理解できるようにする。
- 国土数値情報（N02/L01）は出典明示で再配布可能な規約だが、raw の管理を一貫させるため同様に samples 方式とする。

## 2026-06-12 — Step 2: データ取得

### 判断1: 不動産情報ライブラリはブラウザと同一の内部APIで取得した
- サイトのダウンロードボタンが呼ぶ内部API（/in-api/api-aur/aur/csv/transactionPrices）を、サイトが全ブラウザ訪問者に配布している公開キーで呼び出した。これはブラウザでダウンロードする操作のHTTP的な等価物であり、個人APIキー登録は不要。
- 取得物が旧作業の手動ダウンロードCSVとMD5一致したため、「同じ条件・同じデータ」の再現を確認できた。

### 判断2: 2020年の空CSVは削除せず保持する
- 成約価格情報は2021年第1四半期から提供のため、2020年指定では0件（ヘッダーのみ2バイト）になる。
- 「2020年分は公開データに存在しない」という調査結果自体が記録価値を持つため、空ファイルを証跡として data/raw に残し、分析からは除外する。

### 判断3: 2022年CSVのMD5不一致は問題なしと判断
- 旧新で行集合は9,107行完全一致、並び順のみ相違。サーバ側の返却順が非決定的なだけでデータ内容は同一。再取得や調査は不要。

### 判断4: 駅データはN02の2024・2025の両年を保持
- ユーザー指定（2024〜2025）どおり両年取得。駅の新設・廃止・名称変更の差分確認が可能になり、マスタ鮮度の説明材料になる。

### 判断5: 取引CSVは年単位・統合なしで保存
- ユーザー指定どおり。rawは「取得単位のまま」が原則で、統合はstaging以降のdbtで行う方が再現性・説明性が高い。

## 2026-06-12 — Step 0: プロジェクト作成

### 判断1: 新ディレクトリ `osaka-fudosan-dataanalysis` でゼロから再構築する
- 旧実装 `osaka-condo-station-opportunity-analysis` が存在するが、v9 手順書では設計が更新されている（予測特徴量とスコア用項目の分離、PART 17 のモデル比較・配点感度分析の追加）。
- 旧フォルダは参照のみとし、変更・削除はしない。再利用できる SQL/設定は今後 Step ごとに個別判断する。

### 判断2: CLAUDE.md に v9 の特徴量/スコア分離ルールを明記した
- v9 の最重要設計（公示価格・station_transaction_count・station_price_iqr・renovation_unknown_flag を予測モデルに入れない）は、実装中に忘れると二重カウントのバグになるため、毎セッション読み込まれる CLAUDE.md に固定した。

### 判断3: .gitignore に *.hyper / *.twbx を追加した
- 手順書の必須除外（raw・認証情報・profiles.yml）に加え、Tableau 抽出ファイルは大容量バイナリで raw データを内包しうるため、事故防止として除外に含めた。

### 判断4: profiles.example.yml は oauth を既定の例にした
- 個人開発では service-account JSON ファイルの取り回しが漏えいリスクになるため、例示は oauth を既定にし、keyfile はリポジトリ外に置く旨をコメントで明記した。
